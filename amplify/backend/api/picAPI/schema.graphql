type Picture @model
  @auth(rules: [
    { allow: owner, ownerField: "owner" },
    { allow: public, operations: [read] },
    { allow: private, operations: [read] }
  ]) {
  id: ID!
  title: String!
  description: String
  owner: String!
  filepath: String!
  likecount: Int!
}